# Query Workflow

1. 先讀 `index.md` 找 topic entry point
2. 讀相關 `wiki/maps/` 與 `wiki/concepts/`
3. 必要時回到 `wiki/articles/`
4. 若仍不夠，再回看 `raw/`
5. 產出回答、比較、提案或下一步
6. 若結果可重用，回寫到對應的 concept 或 map page

優先順序是：

- map
- concept
- article
- raw

原因是這個 repo 的目的不是每次都從原始來源重讀，而是讓較穩定的 compiled layer 承擔大部分查詢工作。
